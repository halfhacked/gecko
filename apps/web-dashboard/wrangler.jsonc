{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "gecko",
  "main": ".open-next/worker.js",
  "compatibility_date": "2025-09-23",
  "compatibility_flags": ["nodejs_compat"],
  // Re-add `"limits": { "cpu_ms": 30000 }` after upgrading to Workers Paid.
  // SSR + next-auth + LLM-streaming routes need more than free tier's 10 ms
  // CPU; CPU time excludes time awaiting fetch() to LLM APIs.
  "assets": {
    "binding": "ASSETS",
    "directory": ".open-next/assets"
  },
  "observability": {
    "enabled": true
  },
  // Native D1 binding — replaces the HTTP API path for production traffic.
  // Replace `database_id` with the production database ID before first deploy.
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "gecko-prod",
      "database_id": "178cd509-8ed8-49c4-993a-08c1c5fc36db"
    }
  ]
}
